# Compatibility override point; keep this path stable.

domcom_preseed_endowment_effect = {
    if = {
        limit = {
            is_ai = no
            is_male = yes
            age >= 18
        }
        domcom_seed_from_own_trait_effect = yes
    }
}
